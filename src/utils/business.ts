// 业务代码

/**
 * 
 * @param maxAge string yyyy-mm-dd
 * @param minAge string yyyy-mm-dd
 * @param athlete ADMIN.Athlete
 * @param upGroup number 升组 - 暂时不启用
 */
const isAthleteInAgeRange = (maxAge: string, minAge: string, athlete: ADMIN.Athlete, upGroup) => {
  return athlete
}